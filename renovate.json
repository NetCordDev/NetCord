{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":gitSignOff",
    ":disableRateLimiting",
    "helpers:pinGitHubActionDigests",
    "abandonments:recommended"
  ],
  "dependencyDashboard": true,
  "rebaseWhen": "conflicted",
  "nix": {
    "enabled": true
  },
  "lockFileMaintenance": {
    "enabled": true
  },
  "major": {
    "dependencyDashboardApproval": true
  },
  "packageRules": [
    {
      "description": "Group all non-major NuGet updates into one PR",
      "matchManagers": [ "nuget" ],
      "matchUpdateTypes": [ "minor", "patch", "pin", "digest" ],
      "excludePackageNames": [ "dotnet-sdk" ],
      "groupName": "NuGet non-major"
    },
    {
      "description": "Tooling update",
      "matchManagers": [ "nuget", "nix" ],
      "matchUpdateTypes": [ "major", "minor", "patch", "pin", "digest", "lockFileMaintenance" ],
      "matchPackageNames": [ "dotnet-sdk", "https://github.com/NixOS/nixpkgs" ],
      "groupName": "tooling",
      "dependencyDashboardApproval": true
    },
    {
      "description": "Group all non-major npm updates into one PR",
      "matchManagers": [ "npm" ],
      "matchUpdateTypes": [ "minor", "patch", "pin", "digest" ],
      "groupName": "npm non-major"
    },
    {
      "description": "Group non-major GitHub Actions updates",
      "matchManagers": [ "github-actions" ],
      "matchUpdateTypes": [ "minor", "patch", "pin", "digest" ],
      "groupName": "GitHub Actions non-major"
    }
  ]
}
