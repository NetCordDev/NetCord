namespace NetCord;

/// <summary>
/// Specifies whether a <see cref="PermissionOverwrite"/> applies to a user, or role.
/// </summary>
public enum PermissionOverwriteType
{
    Role = 0,
    User = 1,
}
